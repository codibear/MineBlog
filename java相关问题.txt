byte b1=3,b2=4,b;b=b1+b2;b=3+4;哪句是编译失败的呢？为什么呢？

位运算符的相关问题

补码反码问题

请用最有效率的方式写出计算2乘以8的结果请自己实现两个整数变量的交换^的特点

三目运算符的作用：int x = 1,y=2;	(x>2)?System.out.println("我是"):System.out.println("我是");

switch表示这是switch语句表达式的取值：byte,short,int,charJDK5以后可以是枚举JDK7以后可以是String

if/switch使用场景:

	if语句使用场景：针对结果是boolean类型的判断针对一个范围的判断针对几个常量值的判断
	switch语句使用场景：针对几个常量值的判断

for/while使用场景：

	for循环适合针对一个范围判断进行操作
	while循环适合判断次数不明确操作

循环使用选择：写程序优先考虑for循环，再考虑while循环，最后考虑do…while循环。如下代码是死循环while(true){}for(;;){}：第二个条件不写默认为true

星星输出的思想，用一个变量去限定另一个变量

数组二分法查找

二维数组杨辉三角

关于数组的最大值最小值问题：
	
	1.利用冒泡排序法，然后去第一个或者是length-1个
	2.把第一个定义为最大，或者最小，然后只用一层循环，用它比较
//定义参照物
		int max = arr[0];

		//循环遍历从1开始元素
		for(int x=1; x<arr.length; x++) {
			//比较，把大的留下来
			if(arr[x] > max) {
				max = arr[x];
			}
		}

		//返回最大值
		return max;

数组倒序：如何通过两层for循环来实现

匿名对象：new 对象名()用来当作参数，或者只调用一次的时候用

继承///构造方法-----
C:如果父类没有无参构造方法，那么，我们只能
			a:通过super去访问父类的带参构造方法。
			b:通过this去访问本类的其他构造方法。

final修饰的方法会影响其重载或重写么？
			
			影响重写   不影响重载

抽象类的子类	要么是抽象类	要么重写抽象类中的所有抽象方法

构造方法的作用是什么呢?		用于子类访问父类数据的初始化

abstract不能和哪些关键字共存private	冲突final	冲突	static	无意义

接口和抽象类的感悟：
	抽象类：是其他类继承的模板（子类重写抽象方法）
	接口：	为了是在此拓展，类的方法、属性的多样性
	
接口的子类	要么是抽象类	要么重写接口中的所有抽象方法

内部类的访问特点：内部类可以直接访问外部类的成员，包括私有。外部类要访问内部类的成员，必须创建对象。

内部类不懂===============

局部变量：这个时候，局部对象并没有立马从堆内存中消失，还要使用那个变量。为了让数据还能继续被使用，就用fianl修饰，这样，在堆内存里面存储的其实是一个常量值